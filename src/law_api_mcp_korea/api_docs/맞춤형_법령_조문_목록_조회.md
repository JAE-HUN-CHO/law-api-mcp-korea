# 맞춤형 법령 조문 목록 조회

## 1. API 명칭 및 기본 설명
- **API 명칭**: 맞춤형 법령 조문 목록 조회
- **기본 설명**: 맞춤형 분류코드(vcode)에 따른 법령 조문 목록을 조회하는 API

## 2. 가이드 페이지 URL
- https://open.law.go.kr/LSO/openApi/guideResult.do?htmlName=custLsJoListGuide

## 3. 요청 URL (Endpoint)
- http://www.law.go.kr/DRF/lawSearch.do?target=couseLs

## 4. 요청 변수 (Request Parameters) 명세표
| 변수명 | 타입/필수 여부 | 설명 |
| --- | --- | --- |
| OC | string(필수) | 신청한 API인증값 |
| target | string : couseLs(필수) | 서비스 대상 |
| type | char(필수) | 출력 형태 : HTML/XML/JSON |
| vcode | string | 분류코드(필수) 법령은 L로 시작하는 14자리 코드(L0000000000001) |
| lj | string(필수) | 조문여부 (jo 고정) |
| display | int | 검색된 결과 개수 (default=20 max=100) |
| page | int | 검색 결과 페이지 (default=1) |
| popYn | string | 상세화면 팝업창 여부(팝업창으로 띄우고 싶을 때만 'popYn=Y') |

## 5. 출력 결과 (Response Elements) 명세표
| 필드명 | 타입 | 설명 |
| --- | --- | --- |
| target | string | 검색서비스 대상 |
| vcode | string | 분류코드 |
| section | string | 검색범위 |
| totalCnt | int | 페이지당 결과 수 |
| page | int | 페이지당 결과 수 |
| 법령 법령키 | int | 법령 법령키 |
| 법령ID | int | 법령ID |
| 법령명한글 | string | 법령명한글 |
| 공포일자 | int | 공포일자 |
| 공포번호 | int | 공포번호 |
| 제개정구분명 | string | 제개정구분명 |
| 법령구분명 | string | 법령구분명 |
| 시행일자 | int | 시행일자 |
| 조문번호 | int | 조문번호 |
| 조문가지번호 | int | 조문가지번호 |
| 조문제목 | string | 조문제목 |
| 조문시행일자 | int | 조문시행일자 |
| 조문제개정유형 | string | 조문제개정유형 |
| 조문제개정일자문자열 | string | 조문제개정일자문자열 |
| 조문상세링크 | string | 조문상세링크 |
| 조문키 | string | 조문키 |
| 코드 | string | 코드 |

## 6. 에러 코드
- 가이드 페이지에 별도 에러 코드 표는 명시되지 않은 경우가 많습니다. 일반적으로 HTTP 오류, 빈 결과, 또는 XML/JSON 응답의 상태값(제공 시)으로 실패를 판별합니다.

## 7. 비고
- lj=jo 옵션을 추가하여 조문 목록까지 확장 조회합니다.

## 샘플 요청 및 응답 예시
- 아래 예시는 호출 형태를 빠르게 확인하기 위한 템플릿입니다.
- `{...}` 형태의 식별자 값은 실제 조회 대상 값으로 치환해서 사용합니다.

#### 요청 예시
- 예시 1 (XML)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=couseLs&type=XML&lj=jo&vcode=L0000000003384
```
- 예시 2 (HTML)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=couseLs&type=HTML&lj=jo&vcode=L0000000003384
```
- 예시 3 (JSON)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=couseLs&type=JSON&lj=jo&vcode=L0000000003384
```

#### 응답 예시
- XML 구조 예시
```xml
<response>
  <item>
    <field name="target">예시값</field>
    <field name="vcode">예시값</field>
    <field name="section">예시값</field>
    <field name="totalCnt">20</field>
    <field name="법령명한글">자동차관리법</field>
    <field name="조문제목">자동차관리법</field>
  </item>
</response>
```
- JSON 구조 예시
```json
{
  "data": [
    {
      "target": "예시값",
      "vcode": "예시값",
      "section": "예시값",
      "totalCnt": 20,
      "법령명한글": "자동차관리법",
      "조문제목": "자동차관리법"
    }
  ]
}
```

## 8. 메타
- 정리 기준: 국가법령정보 공동활용 OPEN API 활용가이드 구조화 요약
- 근거 수준: direct
