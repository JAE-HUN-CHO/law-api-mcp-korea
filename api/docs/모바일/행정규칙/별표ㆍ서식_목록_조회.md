# 모바일 행정규칙 별표ㆍ서식 목록 조회

## 1. API 명칭 및 기본 설명
- **API 명칭**: 모바일 행정규칙 별표ㆍ서식 목록 조회
- **기본 설명**: 모바일 전용 형식의 행정규칙 별표·서식 목록 조회 API

## 2. 가이드 페이지 URL
- https://open.law.go.kr/LSO/openApi/guideResult.do?htmlName=mobAdmrulBylListGuide

## 3. 요청 URL (Endpoint)
- http://www.law.go.kr/DRF/lawSearch.do?target=admbyl&mobileYn=Y

## 4. 요청 변수 (Request Parameters) 명세표
| 변수명 | 타입/필수 여부 | 설명 |
| --- | --- | --- |
| OC | string(필수) | 신청한 API인증값 |
| target | string : admbyl(필수) | 서비스 대상 |
| search | int | 검색범위 (기본 : 1 별표서식명) 2 : 해당법령검색 3 : 별표본문검색 |
| query | string | 법령명에서 검색을 원하는 질의(default=*) |
| display | int | 검색된 결과 개수 (default=20 max=100) |
| page | int | 검색 결과 페이지 (default=1) |
| sort | string | 정렬옵션 (기본 : lasc 별표서식명 오름차순) ldes 별표서식명 내림차순 |
| org | string | 소관부처별 검색(소관부처코드 제공) |
| knd | string | 별표종류 1 : 별표 2 : 서식 3 : 별지 |
| gana | string | 사전식 검색(ga,na,da…,etc) |
| type | char | 출력 형태 HTML/XML/JSON 생략시 기본값 : XML |
| mobileYn | char : Y (필수) | 모바일여부 |

## 5. 출력 결과 (Response Elements) 명세표
| 필드명 | 타입 | 설명 |
| --- | --- | --- |
| id | int | 검색결과번호 |
| page | int | 결과페이지번호 |
| section | string | 검색범위 |
| target | string | 검색서비스 대상 |
| totalCnt | int | 검색건수 |
| 관련법령ID | int | 관련법령ID |
| 관련행정규칙명 | string | 관련행정규칙명 |
| 관련행정규칙일련번호 | int | 관련행정규칙일련번호 |
| 발령번호 | int | 발령번호 |
| 발령일자 | int | 발령일자 |
| 별표명 | string | 별표명ID |
| 별표번호 | int | 별표번호 |
| 별표서식파일링크 | string | 별표서식파일링크 |
| 별표일련번호 | int | 별표일련번호 |
| 별표종류 | string | 별표종류 |
| 별표행정규칙상세링크 | string | 별표행정규칙상세링크 |
| 소관부처명 | string | 소관부처명 |
| 키워드 | string | 검색어 |
| 행정규칙종류 | string | 행정규칙종류 |

## 6. 에러 코드
- 가이드 페이지에 별도 에러 코드 표는 명시되지 않은 경우가 많습니다. 일반적으로 HTTP 오류, 빈 결과, 또는 XML/JSON 응답의 상태값(제공 시)으로 실패를 판별합니다.

## 샘플 요청 및 응답 예시
- 아래 예시는 호출 형태를 빠르게 확인하기 위한 템플릿입니다.

#### 요청 예시
- 예시 1 (XML)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=admbyl&type=XML&mobileYn=Y
```
- 예시 2 (HTML)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=admbyl&type=HTML&mobileYn=Y
```
- 예시 3 (JSON)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=admbyl&type=JSON&mobileYn=Y
```
- 예시 4 (XML)
```text
http://www.law.go.kr/DRF/lawSearch.do?OC=test&target=admbyl&type=XML&org=1543000&mobileYn=Y
```

#### 응답 예시
- XML 구조 예시
```xml
<response>
  <item>
    <field name="target">예시값</field>
    <field name="키워드">학교</field>
    <field name="section">예시값</field>
    <field name="totalCnt">20</field>
    <field name="소속법령명">자동차관리법</field>
    <field name="상세링크">https://www.law.go.kr/...</field>
  </item>
</response>
```
- JSON 구조 예시
```json
{
  "data": [
    {
      "target": "예시값",
      "키워드": "학교",
      "section": "예시값",
      "totalCnt": 20,
      "소속법령명": "자동차관리법",
      "상세링크": "https://www.law.go.kr/..."
    }
  ]
}
```

## 8. 메타
- 정리 기준: 국가법령정보 공동활용 OPEN API 활용가이드 구조화 요약
- 근거 수준: direct
